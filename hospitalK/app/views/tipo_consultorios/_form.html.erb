<%= form_for(@tipo_consultorio) do |f| %>
  <% if @tipo_consultorio.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_consultorio.errors.count, "error") %> prohibited this tipo_consultorio from being saved:</h2>

      <ul>
      <% @tipo_consultorio.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
