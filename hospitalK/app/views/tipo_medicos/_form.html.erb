<%= form_for(@tipo_medico) do |f| %>
  <% if @tipo_medico.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@tipo_medico.errors.count, "error") %> prohibited this tipo_medico from being saved:</h2>

      <ul>
      <% @tipo_medico.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :nombre %><br>
    <%= f.text_field :nombre %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
